const Redis=require("ioredis"); let redis=null; function getRedis(){if(!redis&&process.env.REDIS_URL)redis=new Redis(process.env.REDIS_URL,{lazyConnect:true});return redis;} async function cacheGet(k){const r=getRedis();if(!r)return null;try{if(r.status==='wait')await r.connect();return await r.get(k);}catch{return null;}} async function cacheSet(k,v,s=60){const r=getRedis();if(!r)return;try{if(r.status==='wait')await r.connect();await r.set(k,v,'EX',s);}catch{}} module.exports={getRedis,cacheGet,cacheSet};
